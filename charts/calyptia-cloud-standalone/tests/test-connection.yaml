apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "calyptia-standalone.fullname" . }}-test-connection"
  labels:
    {{- include "calyptia-standalone.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "calyptia-standalone.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
